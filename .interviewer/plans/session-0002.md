# Examiner Plan — session-0002 / 2026-08-30

Non-scored solved sample. This plan records source grounding and must never be interpreted as candidate performance.

- Target difficulty: 3.2 / 5, median medium
- Stretch items above 3.5: 0
- Target duration: 180 minutes
- Lifecycle: SAMPLE_SOLVED; no active exam, mastery, coverage, streak, XP, or verdict updates

## Bounded source grounding

- DSA: DSA10days `SearchRange`, capacity-to-ship/Koko answer search, and `TimeBasedKeyValueStore`; all three rounds stay inside the exposed binary-search family.
- Java: DSA10days Streams examples for grouping, flattening, frequency aggregation, comparators, and side-effect-free pipelines.
- Concurrency: keyed atomic state and monotonic sequence reasoning already represented across Java/concurrency and market-data material.
- LLD: the exposed Token Bucket Rate Limiter interview core.
- HLD: the exposed Spring Boot Notification Platform with retries and DLQ.
- Spring/infra: Spring Boot projects plus Docker/Kubernetes material and prepared ECR/EKS/RDS/ElastiCache/MSK/S3/IAM mappings.
- Project/behavioral: only repositories and fallback dossier notes; no resume or performance review is assumed.

## Round intent

1. Honest project-depth reconstruction using a project the candidate can substantiate.
2. Streams/functional aggregation with deterministic ordering and parallel-safety discussion.
3. Atomic per-key monotonic sequence acceptance.
4. Binary-search boundary retrieval.
5. Binary search on a monotonic feasibility answer.
6. Binary-search predecessor transfer.
7. Debug identity, overflow, duplicate aggregation, and deterministic ordering.
8. Interview-sized per-key token bucket with injected monotonic time.
9. Notification-platform HLD with retry, idempotency, DLQ, and failure isolation.
10. Mandatory even-session Spring Framework/Boot plus Docker/Kubernetes/AWS operational slice.
11. Evidence-based incident/priority behavioral response.

## Sample-solution policy

This is deliberately a solved example, so `solutions/` contains one editorial per round immediately. Candidate files retain TODOs to preserve a reusable paper; editorials show the post-DONE result. No sealed references are required because no exam is active.
