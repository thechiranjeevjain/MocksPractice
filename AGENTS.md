# Workspace instructions

Always read `.ai/INTERVIEW_AGENT.md` before handling `START MOCK`, `DONE`, or `REBUILD PROFILE`. It is authoritative for this repository.

Also enforce `LEARNING_METHOD.md`: cold attempt before AI, recall before rereading, targeted gap repair, generator-shaped corrections, one-constraint variations, and performance-based measurement.

The three configured preparation repositories are read-only during mock operation. Keep every generated exam, score, profile, snapshot, and review correction inside this repository. Use `G:\TechStudyNotes\review-os` only through its checked-in commands and data contract. Do not stage or push unrelated changes from the noisy parent checkout.
