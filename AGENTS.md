# Workspace instructions

Always read `.ai/INTERVIEW_AGENT.md` before handling `START MOCK`, `DONE`, or `REBUILD PROFILE`. It is authoritative for this repository.

Also enforce `LEARNING_METHOD.md`: cold attempt before AI, recall before rereading, targeted gap repair, generator-shaped corrections, one-constraint variations, and performance-based measurement. Use `.ai/OPERATOR_QUICKCARD.md` for the daily path and `.ai/FILE_MAP.md` for bounded context loading.

All configured reference sources are read-only during mock operation; only best-effort fast-forward pulls are allowed. Keep generated exams under `sessions/`, longitudinal state under `.interviewer/`, and Review OS corrections under `review/`. Use the sibling `review-os` only through its checked-in commands and data contract. Do not stage or push unrelated changes from any parent checkout.
